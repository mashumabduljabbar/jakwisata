import 'package:flutter/material.dart';
import '../../index.dart';

class PreloadPage extends StatelessWidget {
  const PreloadPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const PreloadView();
  }
}
